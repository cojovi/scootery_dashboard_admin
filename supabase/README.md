# Scootery Supabase

Apply migrations to your Supabase project SQL editor (or `supabase db push` once the CLI is linked to a valid project).

- Migration: `migrations/20260811120000_create_menu_items.sql`
- Seed: `seed/menu_items.sql`
